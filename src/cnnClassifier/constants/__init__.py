from pathlib import Path  # Import Path class to work with file paths in an OS-independent way

CONFIG_FILE_PATH = Path("config/config.yaml")  # Create a Path object pointing to the main configuration file
PARAMS_FILE_PATH = Path("params.yaml")  # Create a Path object pointing to the parameters file